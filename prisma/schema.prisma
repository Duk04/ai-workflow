generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Lead {
  id           String   @id @default(uuid())
  name         String
  businessName String?
  contact      String
  message      String
  createdAt    DateTime @default(now())
}
